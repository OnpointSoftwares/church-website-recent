# Church Admin System - Environment Configuration
# Copy this file to .env and update the values for your environment

# Environment (development, staging, production)
ENVIRONMENT=production

# Database Configuration
DB_HOST=localhost
DB_NAME=kazrxdvk_church_management
DB_USER=kazrxdvk_vincent
DB_PASS=@Admin@2025

# Default Admin Account (only used if no admins exist)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=admin123
DEFAULT_ADMIN_EMAIL=admin@church.local

# Security Settings
SESSION_TIMEOUT=3600
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_TIME=900

# Email Configuration
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@church.local
SMTP_FROM_NAME=Church Admin System

# Application Settings
APP_NAME=Church Admin System
APP_URL=https://your-domain.com
TIMEZONE=Africa/Nairobi

# File Upload Settings
MAX_UPLOAD_SIZE=10485760
ALLOWED_FILE_TYPES=pdf,doc,docx,jpg,jpeg,png

# Backup Settings
BACKUP_ENABLED=true
BACKUP_FREQUENCY=daily
BACKUP_RETENTION_DAYS=30
